<view class="page">
  <view wx:if="{{loading}}" class="state-text">加载中...</view>
  <view wx:elif="{{error}}" class="state-text">{{error}}</view>

  <view wx:elif="{{detail}}" class="content">
    <view class="hero">
      <view class="hero-main">
        <view class="word-line">
          <text class="word">{{detail.word}}</text>
          <text class="phonetic">{{detail.phonetic}}</text>
        </view>
      </view>
      <view wx:if="{{detail.audio}}" class="play-btn" bindtap="onPlayAudio">
        <text class="play-icon-text">播</text>
      </view>
    </view>

    <view class="card">
      <view class="card-title">词性与释义</view>
      <view wx:for="{{detail.meanings}}" wx:key="index" class="meaning-item">
        <text wx:if="{{item.partOfSpeech}}" class="part">{{item.partOfSpeech}}</text>
        <text class="definition">{{item.definition}}</text>
      </view>
      <view class="line-text">中文释义：{{detail.chineseMeaning}}</view>
    </view>

    <view wx:if="{{detail.definitionText}}" class="card">
      <view class="card-title">英文释义</view>
      <view class="line-text">{{detail.definitionText}}</view>
    </view>

    <view wx:if="{{detail.detailText}}" class="card">
      <view class="card-title">补充说明</view>
      <view class="line-text">{{detail.detailText}}</view>
    </view>

    <view wx:if="{{detail.forms.length}}" class="card">
      <view class="card-title">词形（时态/单复数）</view>
      <view wx:for="{{detail.forms}}" wx:key="label" class="line-text">{{item.label}}：{{item.value}}</view>
    </view>
  </view>
</view>
